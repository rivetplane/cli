# Test SDK fixture

`rivetplane-sdk-0.1.0-f6110c7.tgz` is the npm tarball built from exact commit `f6110c758081899ffa5ddbc2f9a8a73d3ca4e9d3` in the canonical `rivetplane/typescript-sdk` repository and pull request 1.

SHA-256: `e72f0ec37fcd403d489c644a8b34696a77ee41d85cc5fb4fd436b931d06d3dab`

This temporary fixture lets the CLI test the public `@rivetplane/sdk` 0.1.0 interface while the private repository is not readable by the CLI repository's GitHub Actions token. It is not part of the published CLI tarball. Remove it and install the npm dependency normally after `@rivetplane/sdk` 0.1.0 is public.
